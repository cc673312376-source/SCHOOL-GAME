# 协作开发说明

感谢参与《挚友的1000种死法》开发。这个仓库是无构建步骤的静态网页游戏，核心规则与界面分离，适合多人并行维护。

## 文件分工

- `index.html`：游戏页面结构
- `styles.css`：视觉与手机适配
- `story.css` / `level.css`：世界背景与第一关界面样式
- `game.js`：界面交互、建筑放置和事件表现
- `rules.js`：纯数据与核心规则，可在浏览器和 Node.js 中运行
- `tests/rules.test.js`：规则回归测试

## 推荐工作流

1. 从最新的 `main` 创建功能分支，例如 `feature/new-event`。
2. 一次分支只解决一个清楚的问题。
3. 修改 `rules.js` 时同步补充或更新 `tests/rules.test.js`。
4. 本地运行 `node tests/rules.test.js`。
5. 在浏览器中打开 `index.html`，测试电脑和手机宽度。
6. 推送分支并创建 Pull Request，请另一位维护者检查后再合并。

## 提交信息建议

- `feat: 增加医院建筑`
- `fix: 修正角相邻距离计算`
- `ui: 优化手机端热力图`
- `docs: 更新事件数据说明`
- `test: 增加建筑越界测试`

## 合并前检查

- 页面能正常打开，没有控制台错误。
- 建筑不能越界或重叠。
- 初始布局仍触发四起特殊事件，犯罪预值峰值为 14。
- 推荐三步布局无特殊事件，犯罪预值峰值为 2。
- 规则测试全部通过。
- 不提交私人资料、密码、密钥或本地临时文件。
